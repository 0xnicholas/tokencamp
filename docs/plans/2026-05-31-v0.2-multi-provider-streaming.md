# v0.2 — Multi-Provider + Streaming 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Anthropic 适配（Chat 转换 + Messages Passthrough）、SSE 流式、Cooldown/Retry 容错、模型列表端点。

**Architecture:** ProviderConfig trait 扩展 `chunk_transformer` + `is_passthrough`。StreamWrapper 将 Provider SSE 转为 OpenAI SSE chunk。HttpHandler 新增 `complete_stream()` 和 `complete_raw()`。Router 用 simple_shuffle + Redis cooldown 过滤。Retry 在 Handler 层做指数退避。

**Tech Stack:** Rust 1.85+, Axum, Tokio, reqwest, redis-rs, rand

**关联 Spec:** [docs/specs/2026-05-31-v0.2-multi-provider-streaming.md](../specs/2026-05-31-v0.2-multi-provider-streaming.md)

---

## 文件结构

```
tokencamp-core/src/
├── types.rs              ← [MODIFY] ChatRequest.extra, OpenAiChunk, ChunkChoice, Delta
├── provider/
│   ├── mod.rs            ← [MODIFY] ChunkTransformer type, is_passthrough default
│   ├── openai.rs         ← [MODIFY] chunk_transformer impl
│   └── anthropic.rs      ← [CREATE] AnthropicConfig
├── handler.rs            ← [MODIFY] complete_stream(), complete_raw()
└── streaming.rs          ← [CREATE] StreamWrapper

gateway/
├── Cargo.toml            ← [MODIFY] +redis, +rand
├── src/
│   ├── main.rs           ← [MODIFY] +redis, +router, +messages/models routes
│   ├── config.rs         ← [MODIFY] +RouterSettings, +RedisConfig
│   ├── router/
│   │   ├── mod.rs        ← [CREATE] Router + simple_shuffle
│   │   └── cooldown.rs   ← [CREATE] Redis cooldown
│   ├── resilience/
│   │   └── retry.rs      ← [CREATE] Retry logic
│   └── routes/
│       ├── chat.rs       ← [MODIFY] streaming branch
│       ├── messages.rs   ← [CREATE] Anthropic passthrough
│       └── models.rs     ← [CREATE] GET /v1/models

config/default.yaml       ← [MODIFY] +router_settings, +redis, +anthropic
```

---

### Task 1: 扩展核心类型 + ProviderConfig trait

**Files:**
- Modify: `tokencamp-core/src/types.rs`
- Modify: `tokencamp-core/src/provider/mod.rs`
- Modify: `tokencamp-core/src/lib.rs`

- [ ] **Step 1: 新增 OpenAiChunk 等 SSE 类型到 types.rs**

```rust
// tokencamp-core/src/types.rs 末尾追加
use std::collections::HashMap;

// --- ChatRequest 扩展 ---
// 在现有 ChatRequest struct 末尾追加:
    #[serde(flatten)]
    pub extra: HashMap<String, serde_json::Value>,  // Passthrough 原生字段

// --- 流式 Chunk 类型 ---
#[derive(Debug, Serialize)]
pub struct OpenAiChunk {
    pub id: Option<String>,
    pub object: String,
    pub created: u64,
    pub model: Option<String>,
    pub choices: Vec<ChunkChoice>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub usage: Option<Usage>,
    #[serde(skip)]
    pub done: bool,
}

#[derive(Debug, Serialize)]
pub struct ChunkChoice {
    pub index: u32,
    pub delta: Delta,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub finish_reason: Option<String>,
}

#[derive(Debug, Serialize)]
pub struct Delta {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub role: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
}
```

- [ ] **Step 2: 更新 tokencamp-core/Cargo.toml 添加 serde feature for HashMap**

已满足（serde derive 已启用，serde_json::Value 已可用）。

- [ ] **Step 3: 扩展 ProviderConfig trait**

```rust
// tokencamp-core/src/provider/mod.rs 末尾追加

/// chunk 转换函数: (request, event_type, data) → OpenAiChunk
pub type ChunkTransformer = fn(&ChatRequest, &str, &serde_json::Value) -> OpenAiChunk;

// 在 ProviderConfig trait 内追加两个方法:
    fn is_passthrough(&self) -> bool { false }

    fn chunk_transformer(&self) -> Option<ChunkTransformer> { None }
```

- [ ] **Step 4: 更新 lib.rs exports**

```rust
// tokencamp-core/src/lib.rs 追加
pub use types::{OpenAiChunk, ChunkChoice, Delta};
pub use provider::ChunkTransformer;
```

- [ ] **Step 5: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 6: Commit**

```bash
git add tokencamp-core/src/types.rs tokencamp-core/src/provider/mod.rs tokencamp-core/src/lib.rs
git commit -m "feat(core): add OpenAiChunk types, ChatRequest.extra, extend ProviderConfig trait"
```

---

### Task 2: StreamWrapper 实现

**Files:**
- Create: `tokencamp-core/src/streaming.rs`
- Modify: `tokencamp-core/src/lib.rs`

- [ ] **Step 1: 添加 bytes-stream feature 到 reqwest**

```toml
# tokencamp-core/Cargo.toml
reqwest = { version = "0.12", features = ["json", "stream"] }
```

- [ ] **Step 2: 编写 streaming.rs**

```rust
// tokencamp-core/src/streaming.rs
use std::cell::RefCell;
use std::pin::Pin;
use std::task::{Context, Poll};

use bytes::Bytes;
use futures::Stream;
use pin_project_lite::pin_project;

use crate::provider::ChunkTransformer;
use crate::types::{ChatRequest, ModelResponse, OpenAiChunk};

pin_project! {
    pub struct StreamWrapper<S> {
        #[pin]
        inner: S,
        request: ChatRequest,
        transformer: ChunkTransformer,
        collected_chunks: RefCell<Vec<OpenAiChunk>>,
        sse_buffer: Vec<u8>,
    }
}

impl<S> StreamWrapper<S>
where
    S: Stream<Item = Result<Bytes, reqwest::Error>>,
{
    pub fn new(inner: S, request: ChatRequest, transformer: ChunkTransformer) -> Self {
        Self {
            inner,
            request,
            transformer,
            collected_chunks: RefCell::new(Vec::new()),
            sse_buffer: Vec::new(),
        }
    }

    pub fn build_full_response(&self) -> Option<ModelResponse> {
        let chunks = self.collected_chunks.borrow();
        if chunks.is_empty() {
            return None;
        }
        let last = chunks.last().unwrap();
        let usage = last.usage.clone();
        let content: String = chunks
            .iter()
            .filter_map(|c| c.choices.first())
            .filter_map(|cc| cc.delta.content.clone())
            .collect();

        Some(ModelResponse {
            id: last.id.clone().unwrap_or_default(),
            object: "chat.completion".to_string(),
            created: last.created,
            model: last.model.clone().unwrap_or_default(),
            choices: vec![crate::types::Choice {
                index: 0,
                message: crate::types::Message {
                    role: "assistant".to_string(),
                    content,
                },
                finish_reason: chunks.iter()
                    .filter_map(|c| c.choices.first().and_then(|cc| cc.finish_reason.clone()))
                    .last()
                    .unwrap_or("stop".to_string()),
            }],
            usage: usage.unwrap_or(crate::types::Usage {
                prompt_tokens: 0,
                completion_tokens: 0,
                total_tokens: 0,
            }),
        })
    }
}

impl<S> Stream for StreamWrapper<S>
where
    S: Stream<Item = Result<Bytes, reqwest::Error>>,
{
    type Item = Result<OpenAiChunk, crate::provider::ProviderError>;

    fn poll_next(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Option<Self::Item>> {
        let this = self.project();
        match this.inner.poll_next(cx) {
            Poll::Ready(Some(Ok(bytes))) => {
                this.sse_buffer.extend_from_slice(&bytes);
                // 解析 SSE: "event: xxx\ndata: {...}\n\n"
                if let Some(chunk) = parse_sse(this.sse_buffer, &this.request, *this.transformer) {
                    this.collected_chunks.borrow_mut().push(chunk.clone());
                    Poll::Ready(Some(Ok(chunk)))
                } else {
                    Poll::Pending
                }
            }
            Poll::Ready(Some(Err(e))) => Poll::Ready(Some(Err(e.into()))),
            Poll::Ready(None) => Poll::Ready(None),
            Poll::Pending => Poll::Pending,
        }
    }
}
```

SSE 解析辅助函数实现解析 `event: ...\ndata: ...\n\n` 格式，拆分出 event 类型和 data JSON，调用 transformer。

- [ ] **Step 3: 添加 pin-project-lite 依赖**

```toml
# tokencamp-core/Cargo.toml
pin-project-lite = "0.2"
```

- [ ] **Step 4: 更新 lib.rs**

```rust
pub mod streaming;
```

- [ ] **Step 5: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 6: Commit**

```bash
git add tokencamp-core/
git commit -m "feat(core): implement StreamWrapper for SSE chunk conversion"
```

---

### Task 3: AnthropicConfig 实现

**Files:**
- Create: `tokencamp-core/src/provider/anthropic.rs`
- Modify: `tokencamp-core/src/provider/mod.rs`

- [ ] **Step 1: 编写 anthropic.rs — 结构 + transform_request**

```rust
// tokencamp-core/src/provider/anthropic.rs
use async_trait::async_trait;
use bytes::Bytes;
use reqwest::{header::HeaderMap, Method, StatusCode};
use serde_json::{json, Value};

use super::{ProviderConfig, ProviderError, ChunkTransformer};
use crate::types::{ChatRequest, ModelResponse, OpenAiChunk, ChunkChoice, Delta};

pub struct AnthropicConfig {
    mode: AnthropicMode,
    base_url: String,
    litellm_model: String,   // Anthropic 实际 model ID
}

pub enum AnthropicMode { Chat, Messages }

impl AnthropicConfig {
    pub fn new(base_url: Option<String>, litellm_model: String, mode: AnthropicMode) -> Self {
        Self {
            mode,
            litellm_model,
            base_url: base_url.unwrap_or_else(|| "https://api.anthropic.com".to_string()),
        }
    }
}

#[async_trait]
impl ProviderConfig for AnthropicConfig {
    fn base_url(&self) -> &str { &self.base_url }
    fn is_passthrough(&self) -> bool { matches!(self.mode, AnthropicMode::Messages) }
    fn chunk_transformer(&self) -> Option<ChunkTransformer> {
        match self.mode {
            AnthropicMode::Chat => Some(anthropic_chunk_to_openai),
            AnthropicMode::Messages => None,  // Passthrough 不做 chunk 转换
        }
    }

    async fn transform_request(
        &self, request: &ChatRequest, api_key: &str, headers: &mut HeaderMap,
    ) -> Result<(Method, String, Value), ProviderError> {
        headers.insert("x-api-key", api_key.parse().map_err(|_| ...)?);
        headers.insert("anthropic-version", "2023-06-01".parse().unwrap());
        headers.insert("Content-Type", "application/json".parse().unwrap());

        match self.mode {
            AnthropicMode::Chat => {
                let body = convert_chat_to_anthropic(request, &self.litellm_model);
                Ok((Method::POST, "/v1/messages".to_string(), body))
            }
            AnthropicMode::Messages => {
                let body = serde_json::to_value(request)?;
                Ok((Method::POST, "/v1/messages".to_string(), body))
            }
        }
    }

    async fn transform_response(
        &self, status: StatusCode, _headers: &HeaderMap, body: Bytes,
    ) -> Result<ModelResponse, ProviderError> {
        if !status.is_success() {
            let error: Value = serde_json::from_slice(&body)?;
            let message = error["error"]["message"].as_str().unwrap_or("unknown").to_string();
            return Err(ProviderError::UpstreamError { status: status.as_u16(), message });
        }
        let response: ModelResponse = serde_json::from_slice(&body)?;
        Ok(response)
    }
}

fn convert_chat_to_anthropic(request: &ChatRequest, model: &str) -> Value {
    let mut body = json!({
        "model": model,
        "max_tokens": request.max_tokens.unwrap_or(1024),
        "messages": [],
    });

    // system messages → 顶层 system
    let system_texts: Vec<_> = request.messages.iter()
        .filter(|m| m.role == "system")
        .map(|m| json!({"type": "text", "text": m.content}))
        .collect();
    if !system_texts.is_empty() {
        body["system"] = json!(system_texts);
    }

    // 非 system messages
    let messages: Vec<_> = request.messages.iter()
        .filter(|m| m.role != "system")
        .map(|m| json!({"role": m.role, "content": m.content}))
        .collect();
    body["messages"] = json!(messages);

    if let Some(t) = request.temperature { body["temperature"] = json!(t); }
    if request.stream.unwrap_or(false) { body["stream"] = json!(true); }

    body
}

fn anthropic_chunk_to_openai(
    _request: &ChatRequest, event_type: &str, data: &Value,
) -> OpenAiChunk {
    let now = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH).unwrap().as_secs();

    match event_type {
        "message_start" => OpenAiChunk {
            id: data["message"]["id"].as_str().map(|s| s.to_string()),
            object: "chat.completion.chunk".to_string(),
            created: now,
            model: data["message"]["model"].as_str().map(|s| s.to_string()),
            choices: vec![ChunkChoice {
                index: 0,
                delta: Delta { role: Some("assistant".to_string()), content: None },
                finish_reason: None,
            }],
            usage: None,
            done: false,
        },
        "content_block_delta" => {
            let text = data["delta"]["text"].as_str().unwrap_or("");
            OpenAiChunk {
                id: None, object: "chat.completion.chunk".to_string(), created: now,
                model: None,
                choices: vec![ChunkChoice {
                    index: data["index"].as_u64().unwrap_or(0) as u32,
                    delta: Delta { role: None, content: Some(text.to_string()) },
                    finish_reason: None,
                }],
                usage: None, done: false,
            }
        },
        "message_delta" => {
            let usage = data["usage"].as_object().map(|u| crate::types::Usage {
                prompt_tokens: u.get("input_tokens").and_then(|v| v.as_u64()).unwrap_or(0) as u32,
                completion_tokens: u.get("output_tokens").and_then(|v| v.as_u64()).unwrap_or(0) as u32,
                total_tokens: 0,  // 由调用方求和
            });
            OpenAiChunk {
                id: None, object: "chat.completion.chunk".to_string(), created: now,
                model: None,
                choices: vec![ChunkChoice {
                    index: 0,
                    delta: Delta { role: None, content: None },
                    finish_reason: data["delta"]["stop_reason"].as_str().map(|s| s.to_string()),
                }],
                usage, done: false,
            }
        },
        "message_stop" => OpenAiChunk {
            id: None, object: "chat.completion.chunk".to_string(), created: now,
            model: None, choices: vec![], usage: None, done: true,
        },
        _ => OpenAiChunk {
            id: None, object: "chat.completion.chunk".to_string(), created: now,
            model: None, choices: vec![], usage: None, done: false,
        },
    }
}
```

- [ ] **Step 2: 注册模块**

```rust
// tokencamp-core/src/provider/mod.rs 末尾
pub mod anthropic;
```

- [ ] **Step 3: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add tokencamp-core/src/provider/anthropic.rs tokencamp-core/src/provider/mod.rs
git commit -m "feat(core): implement AnthropicConfig with Chat + Messages dual mode"
```

---

### Task 4: Anthropic 单元测试

**Files:**
- Modify: `tokencamp-core/src/provider/anthropic.rs` (追加 #[cfg(test)])

- [ ] **Step 1: 追加测试模块**

测试 4 个核心场景：
1. Chat 模式 system message 正确提取到顶层
2. Chat 模式请求体含 `model`, `max_tokens`, `messages`
3. Messages 模式请求体原样（直通）
4. SSE chunk 转换: content_block_delta → content chunk

```rust
#[cfg(test)]
mod tests {
    // ... 4 个 #[tokio::test]
}
```

- [ ] **Step 2: 运行测试**

```bash
cd tokencamp && cargo test -p tokencamp-core
```

Expected: 7 tests PASS（3 个已有 + 4 个新增）。

- [ ] **Step 3: Commit**

```bash
git add tokencamp-core/src/provider/anthropic.rs
git commit -m "test(core): add unit tests for AnthropicConfig"
```

---

### Task 5: HttpHandler 扩展

**Files:**
- Modify: `tokencamp-core/src/handler.rs`

- [ ] **Step 1: 新增 complete_stream 和 complete_raw 方法**

`complete_raw()`: 同 `complete()` 但跳过 `transform_response`，直接返回 `Bytes`。
`complete_stream()`: 发送 `stream: true` 请求，返回 `StreamWrapper`。

```rust
impl HttpHandler {
    // 已有: complete()

    pub async fn complete_raw(
        &self, request: &ChatRequest, provider: &dyn ProviderConfig, api_key: &str,
    ) -> Result<Bytes, ProviderError> {
        let mut headers = HeaderMap::new();
        let (method, path, body) = provider.transform_request(request, api_key, &mut headers).await?;
        let url = format!("{}{}", provider.base_url(), path);
        let response = self.client.request(method, &url).headers(headers).json(&body).send().await?;
        let status = response.status();
        let resp_body = response.bytes().await?;
        if !status.is_success() {
            // Passthrough 模式错误：尝试提取 Anthropic 错误消息
            let err: Value = serde_json::from_slice(&resp_body).unwrap_or(json!({"error":{"message":"unknown"}}));
            return Err(ProviderError::UpstreamError {
                status: status.as_u16(),
                message: err["error"]["message"].as_str().unwrap_or("unknown").to_string(),
            });
        }
        Ok(resp_body)
    }

    pub async fn complete_stream(
        &self, request: &ChatRequest, provider: &dyn ProviderConfig, api_key: &str,
    ) -> Result<StreamWrapper<impl Stream<Item = Result<Bytes, reqwest::Error>>>, ProviderError> {
        let mut request = request.clone();
        request.stream = Some(true);  // 强制 stream=true
        let mut headers = HeaderMap::new();
        let (method, path, body) = provider.transform_request(&request, api_key, &mut headers).await?;
        let url = format!("{}{}", provider.base_url(), path);
        let response = self.client.request(method, &url).headers(headers).json(&body).send().await?;
        let status = response.status();
        if !status.is_success() {
            let resp_body = response.bytes().await?;
            let err: Value = serde_json::from_slice(&resp_body).unwrap_or(json!({"error":{"message":"unknown"}}));
            return Err(ProviderError::UpstreamError {
                status: status.as_u16(),
                message: err["error"]["message"].as_str().unwrap_or("unknown").to_string(),
            });
        }
        let transformer = provider.chunk_transformer()
            .ok_or(ProviderError::Timeout)?;  // 不支持流式的 provider
        let stream = response.bytes_stream();
        Ok(StreamWrapper::new(stream, request, transformer))
    }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 3: Commit**

```bash
git add tokencamp-core/src/handler.rs
git commit -m "feat(core): add HttpHandler::complete_raw and complete_stream methods"
```

---

### Task 6: Gateway Config 扩展

**Files:**
- Modify: `gateway/src/config.rs`
- Modify: `config/default.yaml`

- [ ] **Step 1: 添加 RouterSettings + RedisConfig 到 config.rs**

```rust
#[derive(Debug, Deserialize, Clone)]
pub struct RouterSettings {
    #[serde(default = "default_num_retries")]
    pub num_retries: u32,
    #[serde(default = "default_retry_after")]
    pub retry_after: f64,
    #[serde(default = "default_allowed_fails")]
    pub allowed_fails: u32,
    #[serde(default = "default_cooldown_time")]
    pub cooldown_time: u64,
}

fn default_num_retries() -> u32 { 3 }
fn default_retry_after() -> f64 { 0.5 }
fn default_allowed_fails() -> u32 { 3 }
fn default_cooldown_time() -> u64 { 30 }

#[derive(Debug, Deserialize, Clone)]
pub struct RedisConfig {
    pub url: Option<String>,
}
```

`Config` struct 新增：
```rust
#[serde(default)]
pub router_settings: RouterSettings,
#[serde(default)]
pub redis: RedisConfig,
```

- [ ] **Step 2: 更新 config/default.yaml**

```yaml
router_settings:
  num_retries: 3
  retry_after: 0.5
  allowed_fails: 3
  cooldown_time: 30

redis:
  url: ${REDIS_URL}   # 可选，不配则降级内存模式

providers:
  openai:
    api_key: ${OPENAI_API_KEY}
    base_url: "https://api.deepseek.com"
  anthropic:
    api_key: ${ANTHROPIC_API_KEY}
    base_url: "https://api.anthropic.com"

model_list:
  - model_name: deepseek-chat
    provider: openai
  - model_name: claude-sonnet-4-5
    provider: anthropic
    litellm_params:
      model: claude-sonnet-4-5-20250929
```

`ModelEntry` 新增 `litellm_params: Option<LitellmParams>`。

- [ ] **Step 3: 验证配置加载**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add gateway/src/config.rs config/default.yaml
git commit -m "feat(gateway): add RouterSettings, RedisConfig, Anthropic provider config"
```

---

### Task 7: Redis 封装 + CooldownManager

**Files:**
- Create: `gateway/src/router/mod.rs`
- Create: `gateway/src/router/cooldown.rs`
- Modify: `gateway/Cargo.toml`

- [ ] **Step 1: 添加 redis 依赖**

```toml
# gateway/Cargo.toml
redis = { version = "0.27", features = ["tokio-comp", "aio"] }
```

- [ ] **Step 2: 编写 cooldown.rs**

```rust
// gateway/src/router/cooldown.rs
use redis::aio::MultiplexedConnection;
use std::collections::HashMap;
use std::sync::Mutex;

pub enum CooldownBackend {
    Redis(MultiplexedConnection),
    InMemory(Mutex<HashMap<String, std::time::Instant>>),
}

pub struct CooldownManager {
    backend: CooldownBackend,
}

impl CooldownManager {
    pub fn new_redis(conn: MultiplexedConnection) -> Self {
        Self { backend: CooldownBackend::Redis(conn) }
    }

    pub fn new_in_memory() -> Self {
        Self { backend: CooldownBackend::InMemory(Mutex::new(HashMap::new())) }
    }

    pub async fn mark(&self, deployment_id: &str, ttl_secs: u64) -> Result<(), String> {
        match &self.backend {
            CooldownBackend::Redis(conn) => {
                let mut conn = conn.clone();
                redis::cmd("SETEX")
                    .arg(format!("deployment:{}:cooldown", deployment_id))
                    .arg(ttl_secs).arg("1")
                    .query_async(&mut conn).await.map_err(|e| e.to_string())
            }
            CooldownBackend::InMemory(map) => {
                map.lock().unwrap().insert(
                    deployment_id.to_string(),
                    std::time::Instant::now() + std::time::Duration::from_secs(ttl_secs),
                );
                Ok(())
            }
        }
    }

    pub async fn is_cooling_down(&self, deployment_id: &str) -> bool {
        match &self.backend {
            CooldownBackend::Redis(conn) => {
                let mut conn = conn.clone();
                redis::cmd("EXISTS")
                    .arg(format!("deployment:{}:cooldown", deployment_id))
                    .query_async(&mut conn).await.unwrap_or(0i32) > 0
            }
            CooldownBackend::InMemory(map) => {
                if let Some(expiry) = map.lock().unwrap().get(deployment_id) {
                    *expiry > std::time::Instant::now()
                } else { false }
            }
        }
    }
}
```

- [ ] **Step 3: 编写 router/mod.rs**

```rust
// gateway/src/router/mod.rs
pub mod cooldown;

use rand::seq::SliceRandom;
use crate::config::ModelEntry;

pub struct Router {
    cooldown: cooldown::CooldownManager,
}

impl Router {
    pub fn new(cooldown: cooldown::CooldownManager) -> Self { Self { cooldown } }

    pub async fn select_deployment(
        &self, model_name: &str, deployments: &[ModelEntry],
    ) -> Result<&ModelEntry, RouterError> {
        // 过滤 cooldown
        let available: Vec<_> = deployments.iter()
            .filter(|d| {
                let id = format!("{}:{}", d.provider, d.model_name);
                !tokio::task::block_in_place(|| {
                    tokio::runtime::Handle::current().block_on(self.cooldown.is_cooling_down(&id))
                })
            })
            .collect();

        if available.is_empty() {
            return Err(RouterError::NoAvailableDeployment);
        }

        // simple-shuffle
        let mut rng = rand::thread_rng();
        Ok(available.choose(&mut rng).unwrap())
    }
}
```

- [ ] **Step 4: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 5: Commit**

```bash
git add gateway/src/router/ gateway/Cargo.toml
git commit -m "feat(gateway): implement CooldownManager (Redis+InMemory) and Router with simple_shuffle"
```

---

### Task 8: Retry 逻辑

**Files:**
- Create: `gateway/src/resilience/retry.rs`
- Modify: `gateway/src/main.rs` (mod 声明)

- [ ] **Step 1: 添加 rand 依赖**

```toml
# gateway/Cargo.toml
rand = "0.8"
```

- [ ] **Step 2: 编写 retry.rs**

```rust
// gateway/src/resilience/retry.rs
use std::time::Duration;
use tokencamp_core::ProviderError;

pub struct RetryConfig {
    pub num_retries: u32,
    pub retry_after: f64,
}

impl Default for RetryConfig {
    fn default() -> Self {
        Self { num_retries: 3, retry_after: 0.5 }
    }
}

pub fn is_retryable(e: &ProviderError) -> bool {
    matches!(e,
        ProviderError::UpstreamError { status: 429, .. }
        | ProviderError::UpstreamError { status: 500..=599, .. }
        | ProviderError::Timeout
    )
}

pub fn retry_delay(attempt: u32, base: f64) -> Duration {
    let ms = (base * 1000.0 * 2f64.powi(attempt as i32)) as u64
        + rand::random::<u64>() % 100;
    Duration::from_millis(ms)
}
```

- [ ] **Step 3: 在 main.rs 声明模块**

```rust
mod resilience;
```

- [ ] **Step 4: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 5: Commit**

```bash
git add gateway/src/resilience/ gateway/Cargo.toml gateway/src/main.rs
git commit -m "feat(gateway): implement RetryConfig with exponential backoff"
```

---

### Task 9: resolve_provider 扩展 + main.rs 集成

**Files:**
- Modify: `gateway/src/main.rs`

- [ ] **Step 1: 重写 resolve_provider 支持多 provider**

```rust
impl AppState {
    pub fn resolve_provider(
        &self, model_name: &str,
    ) -> Result<(Box<dyn ProviderConfig>, String), error::AppError> {
        // 查找所有匹配的 deployments
        let candidates: Vec<_> = self.config.model_list.iter()
            .filter(|m| m.model_name == model_name)
            .collect();

        if candidates.is_empty() {
            return Err(error::AppError::ModelNotFound { model: model_name.to_string() });
        }

        // Router 选一个
        let entry = if candidates.len() > 1 {
            self.router.select_deployment(model_name, &candidates).await
                .map_err(|_| error::AppError::ModelNotFound { model: model_name.to_string() })?
        } else {
            candidates[0]
        };

        let provider = self.config.providers.get(&entry.provider)
            .ok_or(error::AppError::ProviderNotFound { provider: entry.provider.clone() })?;

        let api_key = provider.api_key.clone();
        let base_url = provider.base_url.clone();
        let litellm_model = entry.litellm_params.as_ref()
            .map(|p| p.model.clone())
            .unwrap_or_else(|| model_name.to_string());

        let config: Box<dyn ProviderConfig> = match entry.provider.as_str() {
            "openai" => Box::new(OpenAiConfig::new(base_url)),
            "anthropic" => Box::new(AnthropicConfig::new(base_url, litellm_model, AnthropicMode::Chat)),
            other => return Err(error::AppError::ProviderNotFound { provider: other.to_string() }),
        };

        Ok((config, api_key))
    }
}
```

- [ ] **Step 2: 更新 main() 初始化 Redis + Router**

```rust
#[tokio::main]
async fn main() {
    let config = Arc::new(config::load("config/default.yaml").expect("Failed to load config"));
    let handler = Arc::new(HttpHandler::new());

    // Redis → CooldownManager
    let cooldown = match &config.redis.url {
        Some(redis_url) => {
            let client = redis::Client::open(redis_url.as_str()).unwrap();
            let conn = client.get_multiplexed_async_connection().await.unwrap();
            CooldownManager::new_redis(conn)
        }
        None => CooldownManager::new_in_memory(),
    };

    let router = Arc::new(Router::new(cooldown));
    let retry_config = config.router_settings.clone();

    let state = AppState { config, handler, router, retry_config: Arc::new(retry_config.into()) };

    // 注册路由: chat, messages, models
    let app = Router::new()
        .route("/v1/chat/completions", post(routes::chat::chat_completions))
        .route("/v1/messages", post(routes::messages::anthropic_messages))
        .route("/v1/models", get(routes::models::list_models))
        .with_state(state);

    // ... serve ...
}
```

- [ ] **Step 3: AppState 扩展**

```rust
pub struct AppState {
    pub config: Arc<config::Config>,
    pub handler: Arc<HttpHandler>,
    pub router: Arc<Router>,
    pub retry_config: Arc<RetryConfig>,
}
```

- [ ] **Step 4: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 5: Commit**

```bash
git add gateway/src/main.rs
git commit -m "feat(gateway): extend resolve_provider for multi-provider, integrate Router+Redis"
```

---

### Task 10: 新路由 + chat 流式

**Files:**
- Create: `gateway/src/routes/messages.rs`
- Create: `gateway/src/routes/models.rs`
- Modify: `gateway/src/routes/chat.rs`

- [ ] **Step 1: 编写 messages.rs**（见 §4.1 spec）

- [ ] **Step 2: 编写 models.rs**（见 §4.2 spec）

- [ ] **Step 3: 修改 chat.rs 增加流式分支**

```rust
pub async fn chat_completions(...) -> Result<..., AppError> {
    if request.stream.unwrap_or(false) {
        // 流式路径
        let (provider_config, api_key) = state.resolve_provider(&request.model)?;
        let stream = state.handler.complete_stream(&request, provider_config.as_ref(), &api_key).await?;
        // 返回 SSE
        let sse_stream = stream.map(|chunk| {
            let json = serde_json::to_string(&chunk?).unwrap();
            Ok::<_, Infallible>(axum::response::sse::Event::default().data(json))
        });
        Ok(Sse::new(sse_stream).into_response())
    } else {
        // 非流式路径（已有）
        // ...
    }
}
```

注意：流式返回值类型不同（`Sse` vs `Json`），需要返回 `Response` 或独立的 handler 函数。建议拆为 `chat_completions`（非流式）和 `chat_completions_stream`（流式），根据 `stream` flag 在路由层分发。

- [ ] **Step 4: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 5: Commit**

```bash
git add gateway/src/routes/
git commit -m "feat(gateway): add /v1/messages, /v1/models, streaming chat completions"
```

---

### Task 11: 集成测试

- [ ] **Step 1: Chat Completions → Anthropic**

```bash
curl -X POST http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer sk-tc-dev-key-1" \
  -d '{"model":"claude-sonnet-4-5","messages":[{"role":"user","content":"say hi"}]}'
```

Expected: 200 + OpenAI 格式响应。

- [ ] **Step 2: Messages Passthrough**

```bash
curl -X POST http://localhost:3000/v1/messages \
  -H "Authorization: Bearer sk-tc-dev-key-1" \
  -d '{"model":"claude-sonnet-4-5","max_tokens":100,"messages":[{"role":"user","content":"say hi"}]}'
```

Expected: 200 + Anthropic 原生响应。

- [ ] **Step 3: SSE Streaming**

```bash
curl -N -X POST http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer sk-tc-dev-key-1" \
  -d '{"model":"claude-sonnet-4-5","stream":true,"messages":[{"role":"user","content":"write haiku"}]}'
```

Expected: 逐 chunk SSE 事件。

- [ ] **Step 4: GET /v1/models**
- [ ] **Step 5: 无 Redis 降级模式**

---

### Task 12: 文档更新 + commit

---

## 完成检查清单

- [ ] `cargo build` 全 workspace 通过
- [ ] `cargo test -p tokencamp-core` 7+ tests 通过
- [ ] Anthropic Chat Completions 端到端
- [ ] Anthropic Messages Passthrough 端到端
- [ ] SSE streaming 端到端
- [ ] GET /v1/models
- [ ] Cooldown 触发验证
- [ ] Redis 可选（无 REDIS_URL 降级内存）
