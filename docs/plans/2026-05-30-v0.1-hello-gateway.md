# v0.1 — Hello Gateway 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建最小可用 LLM API Gateway — 单模型透传，证明 Rust + Axum 技术栈可行性。

**Architecture:** Cargo workspace 分 `gateway/`（Axum 应用）和 `tokencamp-core/`（纯 Rust crate）。Gateway 负责认证和请求分发，Core 负责 Provider 适配和 HTTP 编排。v0.1 仅接入 OpenAI。

**Tech Stack:** Rust 1.85+, Axum, Tokio, reqwest, serde, serde_yaml, async-trait, thiserror

**关联 Spec:** [docs/specs/2026-05-30-v0.1-hello-gateway.md](../specs/2026-05-30-v0.1-hello-gateway.md)

---

## 文件结构

```
tokencamp/                          ← 仓库根目录
├── Cargo.toml                      ← [MODIFY] workspace 声明
├── config/
│   └── default.yaml                ← [CREATE] v0.1 配置
├── gateway/
│   ├── Cargo.toml                  ← [CREATE] 依赖 axum, tokio, serde_yaml, tokencamp-core
│   └── src/
│       ├── main.rs                 ← [CREATE] 启动入口
│       ├── config.rs               ← [CREATE] Config 结构 + YAML 加载
│       ├── auth.rs                 ← [CREATE] KeyAuth extractor
│       ├── error.rs                ← [CREATE] AppError + IntoResponse
│       └── routes/
│           └── chat.rs             ← [CREATE] POST /v1/chat/completions
├── tokencamp-core/
│   ├── Cargo.toml                  ← [CREATE] 依赖 reqwest, serde, async-trait, thiserror, bytes
│   └── src/
│       ├── lib.rs                  ← [CREATE] re-export 公共类型
│       ├── types.rs                ← [CREATE] ChatRequest, ModelResponse, Message 等
│       ├── handler.rs              ← [CREATE] HttpHandler
│       └── provider/
│           ├── mod.rs              ← [CREATE] ProviderConfig trait + ProviderError
│           └── openai.rs           ← [CREATE] OpenAiConfig 实现
├── docker/
│   ├── Dockerfile                  ← [CREATE] 生产镜像
│   └── docker-compose.yml          ← [CREATE] 开发环境
└── README.md                       ← [CREATE/MODIFY] 项目说明
```

---

### Task 1: 创建 Cargo workspace 骨架

**Files:**
- Create: `Cargo.toml` (根), `gateway/Cargo.toml`, `gateway/src/main.rs` (占位), `tokencamp-core/Cargo.toml`, `tokencamp-core/src/lib.rs` (占位)

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p tokencamp/gateway/src/routes
mkdir -p tokencamp/tokencamp-core/src/provider
mkdir -p tokencamp/config
mkdir -p tokencamp/docker
```

创建路由模块入口文件（占位，后续 Task 10 填充）：

```rust
// gateway/src/routes/mod.rs
// v0.1: 模块将在 Task 10 中注册
```

- [ ] **Step 2: 创建根 Cargo.toml**

```toml
# Cargo.toml
[workspace]
members = ["gateway", "tokencamp-core"]
resolver = "2"
```

- [ ] **Step 3: 创建 tokencamp-core/Cargo.toml**

```toml
# tokencamp-core/Cargo.toml
[package]
name = "tokencamp-core"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"
thiserror = "2"
bytes = "1"
```

- [ ] **Step 4: 创建 tokencamp-core/src/lib.rs 占位**

```rust
// tokencamp-core/src/lib.rs
pub mod types;
pub mod provider;
pub mod handler;
```

- [ ] **Step 5: 创建 gateway/Cargo.toml**

```toml
# gateway/Cargo.toml
[package]
name = "gateway"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8", features = ["macros"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "2"
tokencamp-core = { path = "../tokencamp-core" }
```

- [ ] **Step 6: 创建 gateway/src/main.rs 占位**

```rust
// gateway/src/main.rs
fn main() {
    println!("Tokencamp v0.1");
}
```

- [ ] **Step 7: 验证编译**

```bash
cd tokencamp && cargo build
```

Expected: 两个 crate 编译成功（main.rs 打印占位信息）。

- [ ] **Step 8: 创建 config/default.yaml**

```yaml
# config/default.yaml
server:
  host: "0.0.0.0"
  port: 3000

auth:
  api_keys:
    - "sk-tc-dev-key-1"
    - "sk-tc-dev-key-2"

providers:
  openai:
    api_key: ${OPENAI_API_KEY}
    base_url: "https://api.openai.com"

model_list:
  - model_name: gpt-4o-mini
    provider: openai
```

- [ ] **Step 9: Commit**

```bash
git add Cargo.toml Cargo.lock gateway/ tokencamp-core/ config/
git commit -m "feat: scaffold Cargo workspace with gateway and tokencamp-core crates"
```

---

### Task 2: 定义核心类型 (types.rs)

**Files:**
- Create: `tokencamp-core/src/types.rs`
- Modify: `tokencamp-core/src/lib.rs`

- [ ] **Step 1: 编写 types.rs**

```rust
// tokencamp-core/src/types.rs
use serde::{Deserialize, Serialize};

#[derive(Debug, Deserialize)]
pub struct ChatRequest {
    pub model: String,
    pub messages: Vec<Message>,
    #[serde(default)]
    pub temperature: Option<f32>,
    #[serde(default)]
    pub max_tokens: Option<u32>,
    #[serde(default)]
    pub stream: Option<bool>,
}

#[derive(Debug, Deserialize, Serialize, Clone)]
pub struct Message {
    pub role: String,
    pub content: String,
}

#[derive(Debug, Serialize)]
pub struct ModelResponse {
    pub id: String,
    pub object: String,
    pub created: u64,
    pub model: String,
    pub choices: Vec<Choice>,
    pub usage: Usage,
}

#[derive(Debug, Serialize)]
pub struct Choice {
    pub index: u32,
    pub message: Message,
    pub finish_reason: String,
}

#[derive(Debug, Serialize)]
pub struct Usage {
    pub prompt_tokens: u32,
    pub completion_tokens: u32,
    pub total_tokens: u32,
}
```

- [ ] **Step 2: 更新 lib.rs exports**

```rust
// tokencamp-core/src/lib.rs
pub mod types;
pub mod provider;
pub mod handler;

pub use types::{ChatRequest, Message, ModelResponse, Choice, Usage};
```

- [ ] **Step 3: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add tokencamp-core/src/types.rs tokencamp-core/src/lib.rs
git commit -m "feat(core): define ChatRequest, ModelResponse and related types"
```

---

### Task 3: 定义 ProviderConfig trait + ProviderError

**Files:**
- Create: `tokencamp-core/src/provider/mod.rs`

- [ ] **Step 1: 编写 provider/mod.rs**

```rust
// tokencamp-core/src/provider/mod.rs
use async_trait::async_trait;
use bytes::Bytes;
use reqwest::{header::HeaderMap, Method, StatusCode};

use crate::types::{ChatRequest, ModelResponse};

#[derive(Debug, thiserror::Error)]
pub enum ProviderError {
    #[error("upstream error: status={status}, message={message}")]
    UpstreamError { status: u16, message: String },

    #[error("HTTP error: {0}")]
    HttpError(#[from] reqwest::Error),

    #[error("serialization error: {0}")]
    SerializationError(#[from] serde_json::Error),

    #[error("timeout")]
    Timeout,
}

#[async_trait]
pub trait ProviderConfig: Send + Sync {
    async fn transform_request(
        &self,
        request: &ChatRequest,
        api_key: &str,
        headers: &mut HeaderMap,
    ) -> Result<(Method, String, serde_json::Value), ProviderError>;

    async fn transform_response(
        &self,
        status: StatusCode,
        headers: &HeaderMap,
        body: Bytes,
    ) -> Result<ModelResponse, ProviderError>;

    fn base_url(&self) -> &str;
}
```

- [ ] **Step 2: 向 lib.rs 暴露 ProviderError 和 ProviderConfig**

```rust
// tokencamp-core/src/lib.rs 追加
pub use provider::{ProviderConfig, ProviderError};
```

- [ ] **Step 3: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add tokencamp-core/src/provider/mod.rs tokencamp-core/src/lib.rs
git commit -m "feat(core): define ProviderConfig trait and ProviderError"
```

---

### Task 4: 实现 OpenAiConfig

**Files:**
- Create: `tokencamp-core/src/provider/openai.rs`

- [ ] **Step 1: 编写 openai.rs**

```rust
// tokencamp-core/src/provider/openai.rs
use async_trait::async_trait;
use bytes::Bytes;
use reqwest::{header::HeaderMap, Method, StatusCode};

use super::{ProviderConfig, ProviderError};
use crate::types::{ChatRequest, ModelResponse};

pub struct OpenAiConfig {
    base_url: String,
}

impl OpenAiConfig {
    pub fn new(base_url: Option<String>) -> Self {
        Self {
            base_url: base_url.unwrap_or_else(|| "https://api.openai.com".to_string()),
        }
    }
}

#[async_trait]
impl ProviderConfig for OpenAiConfig {
    fn base_url(&self) -> &str {
        &self.base_url
    }

    async fn transform_request(
        &self,
        request: &ChatRequest,
        api_key: &str,
        headers: &mut HeaderMap,
    ) -> Result<(Method, String, serde_json::Value), ProviderError> {
        headers.insert(
            "Authorization",
            format!("Bearer {}", api_key).parse().unwrap(),
        );
        headers.insert("Content-Type", "application/json".parse().unwrap());

        let body = serde_json::to_value(request)?;
        Ok((Method::POST, "/v1/chat/completions".to_string(), body))
    }

    async fn transform_response(
        &self,
        status: StatusCode,
        _headers: &HeaderMap,
        body: Bytes,
    ) -> Result<ModelResponse, ProviderError> {
        if !status.is_success() {
            let error: serde_json::Value = serde_json::from_slice(&body)?;
            let message = error["error"]["message"]
                .as_str()
                .unwrap_or("unknown error")
                .to_string();
            return Err(ProviderError::UpstreamError {
                status: status.as_u16(),
                message,
            });
        }

        let response: ModelResponse = serde_json::from_slice(&body)?;
        Ok(response)
    }
}
```

- [ ] **Step 2: 在 provider/mod.rs 中注册模块**

```rust
// tokencamp-core/src/provider/mod.rs 末尾添加
pub mod openai;
```

- [ ] **Step 3: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add tokencamp-core/src/provider/openai.rs tokencamp-core/src/provider/mod.rs
git commit -m "feat(core): implement OpenAiConfig ProviderConfig"
```

---

### Task 5: 编写 Core 单元测试

**Files:**
- Modify: `tokencamp-core/src/provider/openai.rs` (追加 `#[cfg(test)]`)

- [ ] **Step 1: 追加测试代码到 openai.rs**

```rust
// tokencamp-core/src/provider/openai.rs 末尾追加
#[cfg(test)]
mod tests {
    use super::*;
    use crate::types::{ChatRequest, Message};
    use bytes::Bytes;

    #[tokio::test]
    async fn test_transform_request_adds_auth_header() {
        let config = OpenAiConfig::new(None);
        let request = ChatRequest {
            model: "gpt-4o-mini".to_string(),
            messages: vec![Message {
                role: "user".to_string(),
                content: "Hello".to_string(),
            }],
            temperature: None,
            max_tokens: None,
            stream: None,
        };
        let mut headers = HeaderMap::new();

        let (method, path, body) = config
            .transform_request(&request, "test-key", &mut headers)
            .await
            .unwrap();

        assert_eq!(method, Method::POST);
        assert_eq!(path, "/v1/chat/completions");
        assert_eq!(headers["authorization"], "Bearer test-key");
        assert_eq!(headers["content-type"], "application/json");
        assert_eq!(body["model"], "gpt-4o-mini");
    }

    #[tokio::test]
    async fn test_transform_response_success() {
        let config = OpenAiConfig::new(None);
        let body = Bytes::from(
            r#"{
                "id": "chatcmpl-123",
                "object": "chat.completion",
                "created": 1717000000,
                "model": "gpt-4o-mini",
                "choices": [{
                    "index": 0,
                    "message": {"role": "assistant", "content": "Hi!"},
                    "finish_reason": "stop"
                }],
                "usage": {
                    "prompt_tokens": 10,
                    "completion_tokens": 5,
                    "total_tokens": 15
                }
            }"#,
        );

        let result = config
            .transform_response(StatusCode::OK, &HeaderMap::new(), body)
            .await
            .unwrap();

        assert_eq!(result.id, "chatcmpl-123");
        assert_eq!(result.choices[0].message.content, "Hi!");
        assert_eq!(result.usage.total_tokens, 15);
    }

    #[tokio::test]
    async fn test_transform_response_upstream_error() {
        let config = OpenAiConfig::new(None);
        let body = Bytes::from(
            r#"{"error": {"message": "Rate limit exceeded", "type": "rate_limit"}}"#,
        );

        let err = config
            .transform_response(StatusCode::TOO_MANY_REQUESTS, &HeaderMap::new(), body)
            .await
            .unwrap_err();

        match err {
            ProviderError::UpstreamError { status, message } => {
                assert_eq!(status, 429);
                assert!(message.contains("Rate limit"));
            }
            _ => panic!("expected UpstreamError"),
        }
    }
}
```

- [ ] **Step 2: 运行测试，验证失败/通过**

```bash
cd tokencamp && cargo test -p tokencamp-core
```

Expected: 3 tests PASS.

- [ ] **Step 3: Commit**

```bash
git add tokencamp-core/src/provider/openai.rs
git commit -m "test(core): add unit tests for OpenAiConfig transform_request and transform_response"
```

---

### Task 6: 实现 HttpHandler

**Files:**
- Create: `tokencamp-core/src/handler.rs`

- [ ] **Step 1: 编写 handler.rs**

```rust
// tokencamp-core/src/handler.rs
use std::time::Duration;

use reqwest::header::HeaderMap;

use crate::provider::{ProviderConfig, ProviderError};
use crate::types::{ChatRequest, ModelResponse};

pub struct HttpHandler {
    client: reqwest::Client,
}

impl HttpHandler {
    pub fn new() -> Self {
        Self {
            client: reqwest::Client::builder()
                .timeout(Duration::from_secs(60))
                .build()
                .expect("Failed to create HTTP client"),
        }
    }

    pub async fn complete(
        &self,
        request: &ChatRequest,
        provider: &dyn ProviderConfig,
        api_key: &str,
    ) -> Result<ModelResponse, ProviderError> {
        let mut headers = HeaderMap::new();
        let (method, path, body) = provider
            .transform_request(request, api_key, &mut headers)
            .await?;

        let url = format!("{}{}", provider.base_url(), path);

        let response = self
            .client
            .request(method, &url)
            .headers(headers)
            .json(&body)
            .send()
            .await?;

        let status = response.status();
        let resp_headers = response.headers().clone();
        let resp_body = response.bytes().await?;

        provider
            .transform_response(status, &resp_headers, resp_body)
            .await
    }
}
```

- [ ] **Step 2: 在 lib.rs 暴露 HttpHandler**

```rust
// tokencamp-core/src/lib.rs 追加
pub use handler::HttpHandler;
```

- [ ] **Step 3: 验证编译**

```bash
cd tokencamp && cargo build -p tokencamp-core
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add tokencamp-core/src/handler.rs tokencamp-core/src/lib.rs
git commit -m "feat(core): implement HttpHandler for request orchestration"
```

---

### Task 7: 实现 config.rs — YAML 配置加载 + 环境变量解析

**Files:**
- Create: `gateway/src/config.rs`

- [ ] **Step 1: 添加 serde_yaml 到 gateway/Cargo.toml（已在 Task 1 中添加，确认）**

- [ ] **Step 2: 编写 config.rs**

```rust
// gateway/src/config.rs
use serde::Deserialize;
use std::collections::HashMap;
use std::fs;

#[derive(Debug, Deserialize, Clone)]
pub struct Config {
    pub server: ServerConfig,
    pub auth: AuthConfig,
    pub providers: HashMap<String, ProviderEntry>,
    pub model_list: Vec<ModelEntry>,
}

#[derive(Debug, Deserialize, Clone)]
pub struct ServerConfig {
    pub host: String,
    pub port: u16,
}

#[derive(Debug, Deserialize, Clone)]
pub struct AuthConfig {
    pub api_keys: Vec<String>,
}

#[derive(Debug, Deserialize, Clone)]
pub struct ProviderEntry {
    pub api_key: String,
    pub base_url: Option<String>,
}

#[derive(Debug, Deserialize, Clone)]
pub struct ModelEntry {
    pub model_name: String,
    pub provider: String,
}

/// 加载 YAML 配置，解析 ${ENV_VAR} 占位符
pub fn load(path: &str) -> Result<Config, Box<dyn std::error::Error>> {
    let raw = fs::read_to_string(path)?;
    let resolved = resolve_env_vars(&raw);
    let config: Config = serde_yaml::from_str(&resolved)?;
    Ok(config)
}

fn resolve_env_vars(raw: &str) -> String {
    let mut result = raw.to_string();
    for cap in raw.match_indices("${") {
        let start = cap.0;
        if let Some(end) = raw[start..].find('}') {
            let var_name = &raw[start + 2..start + end];
            if let Ok(val) = std::env::var(var_name) {
                result = result.replace(&format!("${{{}}}", var_name), &val);
            }
        }
    }
    result
}
```

- [ ] **Step 3: 声明 gateway 模块**

```rust
// gateway/src/main.rs 替换占位
mod config;
mod auth;
mod error;
mod routes;

fn main() {
    let config = config::load("config/default.yaml").expect("Failed to load config");
    println!("Config loaded: {:?}", config.server);
}
```

- [ ] **Step 4: 验证配置加载**

```bash
cd tokencamp
OPENAI_API_KEY=sk-test cargo run
```

Expected: 输出 `Config loaded: ServerConfig { host: "0.0.0.0", port: 3000 }`。

- [ ] **Step 5: Commit**

```bash
git add gateway/src/config.rs gateway/src/main.rs
git commit -m "feat(gateway): implement YAML config loader with env var substitution"
```

---

### Task 8: 实现 auth.rs — API Key 认证 Extractor

**Files:**
- Create: `gateway/src/auth.rs`

- [ ] **Step 1: 编写 auth.rs**

```rust
// gateway/src/auth.rs
use axum::{
    extract::FromRequestParts,
    http::{request::Parts, StatusCode},
};
use crate::AppState;

pub struct KeyAuth;

impl FromRequestParts<AppState> for KeyAuth {
    type Rejection = (StatusCode, &'static str);

    async fn from_request_parts(
        parts: &mut Parts,
        state: &AppState,
    ) -> Result<Self, Self::Rejection> {
        let auth_header = parts
            .headers
            .get("authorization")
            .and_then(|v| v.to_str().ok())
            .ok_or((StatusCode::UNAUTHORIZED, "missing authorization header"))?;

        let key = auth_header
            .strip_prefix("Bearer ")
            .ok_or((StatusCode::UNAUTHORIZED, "invalid authorization format"))?;

        if state.config.auth.api_keys.iter().any(|k| k == key) {
            Ok(KeyAuth)
        } else {
            Err((StatusCode::UNAUTHORIZED, "invalid api key"))
        }
    }
}
```

- [ ] **Step 2: 更新 main.rs 引入 AppState**

```rust
// gateway/src/main.rs — 替换为完整版
mod config;
mod auth;
mod error;
mod routes;

use std::sync::Arc;
use tokencamp_core::HttpHandler;

#[derive(Clone)]
pub struct AppState {
    pub config: Arc<config::Config>,
    pub handler: Arc<HttpHandler>,
}

fn main() {
    let config = Arc::new(config::load("config/default.yaml").expect("Failed to load config"));
    let _state = AppState {
        config,
        handler: Arc::new(HttpHandler::new()),
    };
    println!("Auth module compiled successfully");
}
```

- [ ] **Step 3: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
git add gateway/src/auth.rs gateway/src/main.rs
git commit -m "feat(gateway): implement API Key auth extractor (KeyAuth)"
```

---

### Task 9: 实现 error.rs — AppError + IntoResponse

**Files:**
- Create: `gateway/src/error.rs`

- [ ] **Step 1: 编写 error.rs**

```rust
// gateway/src/error.rs
use axum::{
    http::StatusCode,
    response::{IntoResponse, Response},
    Json,
};
use tokencamp_core::ProviderError;

#[derive(Debug, thiserror::Error)]
pub enum AppError {
    #[error("provider error: {0}")]
    Provider(#[from] ProviderError),

    #[error("model not found: {model}")]
    ModelNotFound { model: String },

    #[error("provider not found: {provider}")]
    ProviderNotFound { provider: String },

    #[error("streaming not supported in v0.1")]
    StreamNotSupported,
}

impl IntoResponse for AppError {
    fn into_response(self) -> Response {
        let (status, message) = match &self {
            AppError::Provider(ProviderError::UpstreamError { status: s, message: m }) => {
                (StatusCode::from_u16(*s).unwrap_or(StatusCode::INTERNAL_SERVER_ERROR), m.clone())
            }
            AppError::Provider(ProviderError::Timeout) => {
                (StatusCode::GATEWAY_TIMEOUT, "upstream timeout".to_string())
            }
            AppError::Provider(_) => {
                (StatusCode::BAD_GATEWAY, "upstream error".to_string())
            }
            AppError::ModelNotFound { model } => {
                (StatusCode::BAD_REQUEST, format!("model '{}' not found", model))
            }
            AppError::ProviderNotFound { provider } => {
                (StatusCode::INTERNAL_SERVER_ERROR, format!("provider '{}' not configured", provider))
            }
            AppError::StreamNotSupported => {
                (StatusCode::BAD_REQUEST, "streaming is not supported in this version".to_string())
            }
        };

        let body = serde_json::json!({ "error": { "message": message } });
        (status, Json(body)).into_response()
    }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 3: Commit**

```bash
git add gateway/src/error.rs
git commit -m "feat(gateway): implement AppError with IntoResponse mapping"
```

---

### Task 10: 实现 routes/chat.rs — Chat Completion 路由

**Files:**
- Create: `gateway/src/routes/chat.rs`
- Modify: `gateway/src/main.rs` (追加 resolve_provider 方法 + 启动 axum server)

- [ ] **Step 1: 追加 resolve_provider 到 AppState**

在 main.rs 的 `AppState` 块中追加：

```rust
use axum::{Router, routing::post};
use std::net::SocketAddr;
use tokencamp_core::ProviderConfig;
use tokencamp_core::provider::openai::OpenAiConfig;

impl AppState {
    pub fn resolve_provider(
        &self,
        model_name: &str,
    ) -> Result<(Box<dyn ProviderConfig>, String), error::AppError> {
        let entry = self
            .config
            .model_list
            .iter()
            .find(|m| m.model_name == model_name)
            .ok_or(error::AppError::ModelNotFound {
                model: model_name.to_string(),
            })?;

        let provider = self
            .config
            .providers
            .get(&entry.provider)
            .ok_or(error::AppError::ProviderNotFound {
                provider: entry.provider.clone(),
            })?;

        let base_url = provider.base_url.clone();
        let api_key = provider.api_key.clone();

        let provider_config: Box<dyn ProviderConfig> = match entry.provider.as_str() {
            "openai" => Box::new(OpenAiConfig::new(base_url)),
            other => {
                return Err(error::AppError::ProviderNotFound {
                    provider: other.to_string(),
                })
            }
        };

        Ok((provider_config, api_key))
    }
}
```

- [ ] **Step 2: 注册 chat 子模块**

更新 `gateway/src/routes/mod.rs`（替换占位）：

```rust
// gateway/src/routes/mod.rs
pub mod chat;
```

- [ ] **Step 3: 编写 routes/chat.rs**

```rust
// gateway/src/routes/chat.rs
use axum::{extract::State, Json};
use tokencamp_core::ChatRequest;

use crate::{error::AppError, auth::KeyAuth, AppState};

pub async fn chat_completions(
    State(state): State<AppState>,
    _auth: KeyAuth,
    Json(request): Json<ChatRequest>,
) -> Result<Json<serde_json::Value>, AppError> {
    if request.stream.unwrap_or(false) {
        return Err(AppError::StreamNotSupported);
    }

    let (provider_config, api_key) = state.resolve_provider(&request.model)?;
    let response = state.handler.complete(&request, provider_config.as_ref(), &api_key).await?;
    Ok(Json(serde_json::to_value(response).unwrap()))
}
```

- [ ] **Step 4: 声明 routes 模块 + 注册路由 + 启动 server**

更新 gateway/src/main.rs 的 `main()`：

```rust
#[tokio::main]
async fn main() {
    let config = Arc::new(config::load("config/default.yaml").expect("Failed to load config"));
    let handler = Arc::new(HttpHandler::new());

    let state = AppState { config, handler };

    let app = Router::new()
        .route("/v1/chat/completions", post(routes::chat::chat_completions))
        .with_state(state);

    let addr = SocketAddr::from(([0, 0, 0, 0], 3000));
    println!("Tokencamp v0.1 listening on {}", addr);

    let listener = tokio::net::TcpListener::bind(addr).await.unwrap();
    axum::serve(listener, app).await.unwrap();
}
```

- [ ] **Step 5: 验证编译**

```bash
cd tokencamp && cargo build -p gateway
```

Expected: 编译成功。

- [ ] **Step 6: Commit**

```bash
git add gateway/src/routes/chat.rs gateway/src/routes/mod.rs gateway/src/main.rs
git commit -m "feat(gateway): implement POST /v1/chat/completions with resolve_provider
"
```

---

### Task 11: 集成测试 — 启动 Gateway 手动验证

**Files:**
- 无新文件，手动测试

- [ ] **Step 1: 启动 Gateway**（需要真实 `OPENAI_API_KEY`）

```bash
cd tokencamp
OPENAI_API_KEY=sk-your-real-key cargo run -p gateway
```

Expected: `Tokencamp v0.1 listening on 0.0.0.0:3000`

- [ ] **Step 2: 测试有效请求**

```bash
curl -s -X POST http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer sk-tc-dev-key-1" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"Say hello in one word"}]}' | jq .
```

Expected: 返回 200，含 `choices[0].message.content`。

- [ ] **Step 3: 测试无效 API Key**

```bash
curl -s -X POST http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer sk-wrong-key" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"hi"}]}' | jq .
```

Expected: 返回 401，含 `error.message`。

- [ ] **Step 4: 测试不存在的 model**

```bash
curl -s -X POST http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer sk-tc-dev-key-1" \
  -H "Content-Type: application/json" \
  -d '{"model":"nonexistent-model","messages":[{"role":"user","content":"hi"}]}' | jq .
```

Expected: 返回 400，"model 'nonexistent-model' not found"。

- [ ] **Step 5: 测试流式请求被拒绝**

```bash
curl -s -X POST http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer sk-tc-dev-key-1" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"hi"}],"stream":true}' | jq .
```

Expected: 返回 400，"streaming is not supported"。

- [ ] **Step 6: 全部通过后 Commit**

```bash
# 无需提交新代码，记录测试结果
```

---

### Task 12: Docker 化

**Files:**
- Create: `docker/Dockerfile`
- Create: `docker/docker-compose.yml`

- [ ] **Step 1: 创建 Dockerfile**

```dockerfile
# docker/Dockerfile
FROM rust:1.85-slim-bookworm AS builder
WORKDIR /app
COPY . .
RUN cargo build --release -p gateway

FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y ca-certificates && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY --from=builder /app/target/release/gateway /usr/local/bin/tokencamp-gateway
COPY config/ /app/config/
ENV OPENAI_API_KEY=""
EXPOSE 3000
CMD ["tokencamp-gateway"]
```

- [ ] **Step 2: 创建 docker-compose.yml**

```yaml
# docker/docker-compose.yml
services:
  gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ../config:/app/config:ro
```

- [ ] **Step 3: 验证 Docker 构建**

```bash
cd tokencamp
docker build -f docker/Dockerfile -t tokencamp:v0.1 .
```

Expected: 镜像构建成功。

- [ ] **Step 4: 验证 Docker Compose 启动**

```bash
cd tokencamp/docker
OPENAI_API_KEY=sk-test docker compose up
```

Expected: Gateway 启动成功，可用 curl 测试。

- [ ] **Step 5: Commit**

```bash
git add docker/
git commit -m "feat: add Dockerfile and docker-compose for v0.1"
```

---

### Task 13: 编写 README

**Files:**
- Create/Modify: `README.md`

- [ ] **Step 1: 编写 README.md**

```markdown
# Tokencamp

LLM API Gateway built with Rust + Axum.

## v0.1 — Hello Gateway

Minimal single-provider proxy. Proxies `/v1/chat/completions` to OpenAI.

### Quick Start

1. Set your OpenAI API key:
   ```bash
   export OPENAI_API_KEY=sk-...
   ```

2. Start the gateway:
   ```bash
   cargo run -p gateway
   ```

3. Send a request:
   ```bash
   curl -X POST http://localhost:3000/v1/chat/completions \
     -H "Authorization: Bearer sk-tc-dev-key-1" \
     -H "Content-Type: application/json" \
     -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"Hello"}]}'
   ```

### Docker

```bash
cd docker
OPENAI_API_KEY=sk-... docker compose up
```

### Architecture

- `gateway/` — Axum HTTP server, auth, config
- `tokencamp-core/` — Provider adapters, HTTP handler (no web framework dependency)

### What's Next

See [ROADMAP.md](ROADMAP.md) for version plan.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README with v0.1 quick start"
```

---

## 完成检查清单

- [ ] `cargo build` 全 workspace 通过
- [ ] `cargo test -p tokencamp-core` 全部通过 (3 tests)
- [ ] Gateway 启动成功 (`cargo run -p gateway`)
- [ ] curl 有效请求 → 200 + 正确响应
- [ ] curl 无效 Key → 401
- [ ] curl 不存在的 model → 400
- [ ] curl stream:true → 400
- [ ] docker compose up → Gateway 可访问
