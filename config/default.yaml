# config/default.yaml
server:
  host: "0.0.0.0"
  port: 3000

auth:
  api_keys:
    - "sk-tc-dev-key-1"
    - "sk-tc-dev-key-2"

providers:
  openai:
    api_key: ${OPENAI_API_KEY}
    base_url: "https://api.deepseek.com"

model_list:
  - model_name: deepseek-chat
    provider: openai
